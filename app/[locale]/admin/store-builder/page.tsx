import { StoreBuilderClient } from "./store-builder-client"

export const metadata = {
  title: "Store Builder",
  description: "Build your store with AI assistance",
}

export default function StoreBuilderPage() {
  return <StoreBuilderClient />
}
